# Spring Java App : Songr

Demonstrate basic understanding of Spring MVC.

## API Description 
* getRoot() ``` "/" ```
* getSayHello() ``` "/sayHello" ```
* getHello() ``` "/hello" ```
* capitalize() ``` "/capitalize/{text}" ```
* reverse() ``` "/reverse" ```
* reverseHelper() 




